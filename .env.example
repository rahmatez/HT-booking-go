# ── App ──
APP_ENV=development
APP_PORT=8080
APP_URL=http://localhost:8080
FRONTEND_URL=http://localhost:3000

# ── Database ──
DATABASE_URL=postgres://booking:secret@localhost:5433/booking?sslmode=disable
DATABASE_MAX_OPEN_CONNS=25
DATABASE_MAX_IDLE_CONNS=5

# ── Redis ──
REDIS_URL=redis://localhost:6379/0
REDIS_CACHE_EVENTS_TTL=60s
REDIS_CACHE_AVAIL_TTL=5s
REDIS_CACHE_EVENT_TTL=60s
RATE_LIMIT_LOGIN_PER_MIN=10
RATE_LIMIT_HOLD_PER_MIN=30
RATE_LIMIT_CHECKOUT_PER_MIN=15
RATE_LIMIT_WINDOW=1m

# ── JWT ──
JWT_SECRET=change-me-in-production-use-long-random-string
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=168h

# ── Booking ──
BOOKING_HOLD_TTL=10m
BOOKING_MAX_TICKETS_PER_ORDER=4
BOOKING_MAX_ACTIVE_HOLDS=3
BOOKING_PAYMENT_GRACE_PERIOD=5m

# ── Ticket ──
TICKET_SIGNING_KEY=change-me-ticket-signing-key

# ── Payment (Midtrans) ──
PAYMENT_GATEWAY=midtrans
MIDTRANS_MERCHANT_ID=
MIDTRANS_SERVER_KEY=
MIDTRANS_CLIENT_KEY=
MIDTRANS_IS_PRODUCTION=false

# ── Email ──
EMAIL_PROVIDER=resend
RESEND_API_KEY=
EMAIL_FROM=noreply@eventra.local

# ── Frontend ──
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_MIDTRANS_CLIENT_KEY=
NEXT_PUBLIC_MIDTRANS_IS_PRODUCTION=false

# ── CORS ──
CORS_ALLOWED_ORIGINS=http://localhost:3000

# ── CAPTCHA (Cloudflare Turnstile, optional) ──
TURNSTILE_SECRET_KEY=
NEXT_PUBLIC_TURNSTILE_SITE_KEY=

# ── Observability ──
SENTRY_DSN=
OTEL_EXPORTER_OTLP_ENDPOINT=
